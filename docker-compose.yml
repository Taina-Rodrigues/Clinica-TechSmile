services:
  backend:
    build:
      context: ./backend
    container_name: techsmile-backend
    ports:
      - "3000:3000"

  frontend:
    build:
      context: ./frontend
    container_name: techsmile-frontend
    depends_on:
      - backend
    ports:
      - "8080:80"
